const inserirPedido = require('./inserir');
const editarPedido = require('./editar');
const excluirPedido = require('./excluir');
const listarPedido = require('./listar');

module.exports = {
    inserirPedido,
    editarPedido,
    excluirPedido,
    listarPedido
}